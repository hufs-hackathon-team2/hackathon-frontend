// CY 05 재개 화면 (조건부 자동 · 팝업)
// 휴식기 중 plus log 를 올리거나 퀘스트를 시작하면 노출된다.
import Placeholder from '../components/common/Placeholder';

export default function S40Resume({ navigation }) {
  return (
    <Placeholder
      id="S40"
      title="재개 화면"
      spec="CY 05"
      navigation={navigation}
    />
  );
}
